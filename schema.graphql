type Query {
    hello(name: String): String!
    users: [User!]!
    skills: [String!]!
    posts: [Post!]!
}

type User {
    id: ID!
    name: String!
    username: String!
    email: String
    phone: String!
    website: String!
}

type Post {
    title: String!
    body: String!
}
